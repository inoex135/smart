export class PersonalCalendarOptions {
  static options() {
    const options = {
      pickMode: "multi",
      title: "RANGE",
      canBackwardsSelected: true,
      color: "dark"
    };

    return options;
  }
}
